syntax = "proto3";

package LLVM.TDG;

/**
 * This represents a dynamic BB we traced.
 * @uid is just the UID of the FirstInstruction.
 * @repeats is an optimization for continuous single BB loop.
 */
message DynamicLLVMBasicBlock {
  uint64 uid = 1;
  uint64 repeats = 2;
}

